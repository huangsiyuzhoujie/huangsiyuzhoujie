---
title: python常用知识点
data: 2019-7-24
categories: python
---

```python
astype  # 用于转换数组array的数据类型
tqdm  # 传递可迭代对象，显示进度条
pandas.get_dummies()  # 利用pandas实现one-hot编码
np.logical_or()  # 逻辑或
np.random.beta()  # [0-1]之间的随机分布
```

```python
json.dumps  # 将一个python数据结构转换为json
json.loads  # 将一个json编码的字符串转换回python数据结构
json.dump和json.load  # 用于处理文件
```

